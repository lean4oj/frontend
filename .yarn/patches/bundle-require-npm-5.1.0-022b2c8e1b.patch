diff --git a/dist/index.js b/dist/index.js
index c163321544cba2c785fff8163025bf770d65784c..6e479412be443fbbd2225cc439b233c677291b91 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -86,6 +86,18 @@ var externalPlugin = ({
     name: "bundle-require:external",
     setup(ctx) {
       ctx.onResolve({ filter: /.*/ }, async (args) => {
+        let css = args.path.endsWith(".css");
+        try {
+          const pkg = await import(`${args.path}/package.json`, { with: { type: 'json' }});
+          css = css || pkg.default.main.endsWith('.css');
+        } catch {
+        }
+        if (css) {
+          return { namespace: 'virtual', sideEffects: false, path: 'e' }
+        }
+        if (args.path === '@/appState') {
+          return { namespace: 'virtual', sideEffects: false, path: 'e' }
+        }
         if (match(args.path, external)) {
           return {
             external: true
@@ -108,6 +120,11 @@ var externalPlugin = ({
           external: true
         };
       });
+      ctx.onLoad({ filter: /^e$/ }, async (args) => {
+        if (args.namespace === "virtual") {
+          return { contents: '' }
+        }
+      })
     }
   };
 };
